# TODO #
########

- If item appears in mutliple lists, check if it gets updated everywhere
- Delete saved states if lastUpdate has expired
- Bug: Click on window is delegated through
- Use additional hash with vesselName to ensure it's the same craft
- Remove checklist version
- Rename "visible" to "isVisible"

- Checklists can have a type VAB/SPH/both (default)
- Triggers when a checklist should be auto-selected
- Triggers to check list items automatically
  - "hasPart"
  - "deltaV"
